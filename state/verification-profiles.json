{
  "version": "runx.verification_profile_catalog.v1",
  "repo_defaults": {
    "nilstate/aster": "aster.site-ci"
  },
  "profiles": {
    "aster.site-ci": {
      "repo": "nilstate/aster",
      "description": "Full repo contract checks for aster changes before draft PR publication.",
      "commands": [
        "npm run site:ci"
      ]
    }
  }
}
