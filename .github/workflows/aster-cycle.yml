name: aster-cycle

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  workflow_dispatch:
  schedule:
    - cron: "11 */3 * * *"

permissions:
  actions: write
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: aster-cycle
  cancel-in-progress: true

jobs:
  aster-cycle:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.MATON_GH_TOKEN != '' && secrets.MATON_GH_TOKEN || github.token }}
    steps:
      - name: Check out aster
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Run assess-discover-score-select cycle
        run: |
          mkdir -p .artifacts/aster-cycle
          node scripts/aster-cycle.mjs \
            --repo-root "$GITHUB_WORKSPACE" \
            --repo "${{ github.repository }}" \
            --dispatch \
            --dispatch-ref "${{ github.ref_name || 'main' }}" \
            --output "$GITHUB_WORKSPACE/.artifacts/aster-cycle/result.json" \
            --summary-output "$GITHUB_WORKSPACE/.artifacts/aster-cycle/summary.md"
          cat "$GITHUB_WORKSPACE/.artifacts/aster-cycle/result.json"

      - name: Append workflow summary
        run: cat .artifacts/aster-cycle/summary.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload cycle artifacts
        uses: actions/upload-artifact@v7
        with:
          name: aster-cycle
          path: .artifacts/aster-cycle
