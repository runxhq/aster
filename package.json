{
  "name": "automaton",
  "private": true,
  "version": "0.1.0",
  "description": "Governed dogfood destination for runx live runs.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check.mjs"
  }
}

