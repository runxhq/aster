{
  "name": "automaton",
  "private": true,
  "version": "0.1.0",
  "description": "Governed dogfood destination for runx live runs.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "docs:build": "cd docs && npx -y sourcey build --config sourcey.config.ts -o ../.sourcey/runx-docs",
    "docs:ci": "npm run check && npm run docs:build",
    "site:check": "npm --prefix site run check",
    "site:build": "npm --prefix site run build",
    "site:ci": "npm run check && npm run docs:build && npm run site:check && npm run site:build"
  }
}
