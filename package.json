{
  "name": "aster",
  "private": true,
  "version": "0.1.0",
  "description": "Governed proving-ground destination for runx live runs.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "docs:build": "cd docs && npx -y sourcey build --config sourcey.config.ts -o ../.sourcey/runx-docs",
    "docs:ci": "npm run check && npm run docs:build",
    "shakeout:local": "node scripts/operator-shakeout.mjs",
    "site:check": "npm --prefix site run check",
    "site:build": "npm --prefix site run build",
    "site:ci": "npm run check && npm run site:check && npm run site:build"
  }
}
